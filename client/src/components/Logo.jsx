import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/">
      <span className="font-semibold text-primary text-3xl">OneShop</span>
    </Link>
  );
};

export default Logo;
