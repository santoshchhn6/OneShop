import Slider from "../Slider";
import data from "../../data.json";

const PopularElectronics = () => {
  return <Slider title="Best of Electronics" data={data} />;
};

export default PopularElectronics;
